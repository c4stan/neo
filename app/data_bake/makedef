name = data_bake
code = public, private
defs = public.def
configs = debug, release
output = exe
deps = std
if win32
    dlls = $assimp_dll
    libs = $assimp_lib
    includes = $assimp_include
    bindings = assimp_models
endif
