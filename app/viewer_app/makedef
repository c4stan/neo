name = viewer_app
code = public, private
defs = public.def
output = app
deps = std, rv, xs, xf, se, sm, xi
if win32
    dlls = $assimp_dll
    libs = $assimp_lib
    includes = $assimp_include
endif
if linux
    libs = assimp
endif
