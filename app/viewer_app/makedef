name = viewer_app
code = public, private
defs = public.def
configs = debug, release
output = app
deps = std, rv, xs, xf, se, sm, xi
ignores = particle_pass.c
if win32
    dlls = $assimp_dll
    libs = $assimp_lib
    includes = $assimp_include
    bindings = assimp_models
endif
if linux
    libs = assimp
endif
